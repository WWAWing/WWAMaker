{
  // FIXME: 内部パッケージのビルドでオブジェクトが存在しないコンパイルエラーが100以上発生する
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "module": "CommonJS",
    "rootDir": "./src",
    "outDir": "./lib",
    "isolatedModules": false
  },
  "include": [
    "src/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
